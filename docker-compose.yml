version: '3.8'

services:
  agente-reuniao:
    build: .
    container_name: agente-reuniao-ia
    ports:
      - "8501:8501"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
    restart: unless-stopped